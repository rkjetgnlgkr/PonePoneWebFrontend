server {
    listen 80;
    root /usr/share/nginx/html;

    location = /health {
        default_type text/plain;
        return 200 "ok";
    }

    # 所有路徑（包含 /{username} 動態路由）都 fallback 到 index.html
    location / {
        try_files $uri /index.html;
    }
}
